namespace LimousineBooking.Domain.Enums;

/// <summary>The three pin types the service-area map distinguishes visually (Prompt 19, section 4).</summary>
public enum LocationType
{
    City,
    Airport,
    Destination
}
