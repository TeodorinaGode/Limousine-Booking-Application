/**
 * The real business identity (Prompt 18, section 1) — ROI Limousinen, migrated
 * from the company's existing website (roi-limousinen.ch). This is the one
 * place the display name lives rather than being hard-coded across pages
 * (Prompt 14, section 40). No logo asset was available to migrate (section 25
 * — do not scrape third-party/unowned images), so the header/footer still
 * render this as text.
 */
export const APP_BRAND_NAME = "ROI Limousinen";
export const APP_BRAND_TAGLINE = "Premium / Luxury Chauffeur Service";
