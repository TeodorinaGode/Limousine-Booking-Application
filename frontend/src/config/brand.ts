/**
 * Placeholder brand identity — the real business name/logo isn't known yet, so
 * this is the one place the display name lives rather than being hard-coded
 * across pages (Prompt 14, section 40).
 */
export const APP_BRAND_NAME = "NOIR CHAUFFEUR";
export const APP_BRAND_TAGLINE = "Private Chauffeur Service";
